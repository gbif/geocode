spring:
  datasource:
    url: jdbc:postgresql://localhost/eez
    username: eez_rw
    password: eez_rw
    hikari:
      connectionTimeout: 1000
      idleTimeout: 600000
      maximumPoolSize: 12
      minimumIdle: 2
  liquibase:
    enabled: false
  shapefiles:
    root: ../database/layers/

  jmx:
    enabled: true
